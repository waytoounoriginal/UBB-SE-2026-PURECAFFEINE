<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Property_and_Management.Src.Views.EditGameView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer>
        <StackPanel Width="400" HorizontalAlignment="Center" Spacing="15" Margin="0,40,0,40">
            <TextBlock Text="Edit Listing" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center"/>

            <TextBox Header="Game Name (5-30 chars)*" Text="{x:Bind ViewModel.GameName, Mode=TwoWay}" MaxLength="30"/>

            <NumberBox x:Name="PriceNumberBox" Header="Price*" Value="{x:Bind ViewModel.GamePriceAsDouble, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalAlignment="Stretch"/>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <NumberBox Header="Minimum Players*" Value="{x:Bind ViewModel.MinimumPlayersRequired, Mode=TwoWay}" Minimum="1" Width="190"/>
                <NumberBox Header="Maximum Players*" Value="{x:Bind ViewModel.MaximumPlayersAllowed, Mode=TwoWay}" Minimum="1" Width="200"/>
            </StackPanel>

            <TextBox Header="Description (10-500 chars)*" Text="{x:Bind ViewModel.GameDescription, Mode=TwoWay}" 
                     AcceptsReturn="True" Height="100" MaxLength="500" TextWrapping="Wrap"/>

            <StackPanel Orientation="Horizontal" Spacing="15" Margin="0,10,0,10">
                <Button Content="Upload Game Image (Optional)" Click="UploadImageButton_Click" VerticalAlignment="Center"/>
                <Image x:Name="ImagePreview" Width="80" Height="80" Stretch="UniformToFill"/>
                <TextBlock x:Name="FileNameTextBlock" VerticalAlignment="Center" Foreground="Gray" Text="No image selected."/>
            </StackPanel>

            <ToggleSwitch Header="Listing is Active" IsOn="{x:Bind ViewModel.IsGameActive, Mode=TwoWay}"/>

            <Button Content="Update Listing" Click="SaveButton_Click" Background="DarkGreen" Foreground="White" 
                    HorizontalAlignment="Stretch" Height="40" Margin="0,20,0,0"/>
        </StackPanel>
    </ScrollViewer>
</Page>
